from flask import Flask
import psutil
import time

app = Flask(__name__)

@app.route('/')
def hello():
    cpu = psutil.cpu_percent()
    mem = psutil.virtual_memory().percent
    return f"Hello, World! CPU: {cpu}%, Memory: {mem}%"

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
